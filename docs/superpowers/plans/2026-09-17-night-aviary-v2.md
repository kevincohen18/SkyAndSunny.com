# Night Aviary V2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the rejected brochure-like sandbox with a distinctive, continuous moonlit-aviary diorama while keeping all authentic media and verified links.

**Architecture:** Recompose the existing sandbox HTML and CSS around one continuous habitat with three scenery planes, subject-following portrait masks, and a branch-led scroll narrative. Keep vanilla progressive enhancement and extend its pointer-depth behavior within the strict motion budget.

**Tech Stack:** Semantic HTML5, token-led CSS, inline decorative SVG, vanilla JavaScript, existing local WebP media, local browser verification

**Spec:** `docs/superpowers/specs/2026-09-17-night-aviary-v2.md`

## Global Constraints

- Root `index.html` remains untouched; build only the sandbox preview.
- Use only the five existing authentic WebP derivatives and the exact verified TikTok links.
- No fake metrics, biography facts, videos, play controls, stock imagery, or generated bird imagery.
- The habitat must be visually legible without motion, JavaScript, or remote fonts.
- Preserve semantic navigation, keyboard behavior, skip link, reduced motion, 44px targets, and 320px/200% reflow.
- All decorative SVG is `aria-hidden`, unfocusable, and pointer-inert.
- Existing user/source files remain untouched.

---

### Task 1: Rebuild the sandbox as a moonlit aviary diorama

**Files:**
- Modify: `sandbox/night-aviary/index.html`
- Modify: `sandbox/night-aviary/styles.css`
- Modify: `sandbox/night-aviary/script.js`
- Modify: `tests/verify-night-aviary.sh`
- Replace: `verification/night-aviary-sandbox-desktop.png`
- Replace: `verification/night-aviary-sandbox-tablet.png`
- Replace: `verification/night-aviary-sandbox-mobile.png`

**Interfaces:**
- Consumes: the V2 spec and existing five local media derivatives.
- Preserves: exact post URLs, `#menu-toggle`, `#site-menu`, `#current-year`, JS-off navigation, and real-media mapping checks.
- Produces: decorative `[data-depth]` planes and moment-level twig hooks for restrained progressive motion.

- [ ] Rework the structural verifier first so it fails on V1-specific removed strings/classes and requires V2 moon, ribs, canopy, branch path, and depth-plane hooks.
- [ ] Run the verifier and confirm V1 fails.
- [ ] Rewrite the hero into a three-plane, edge-to-edge habitat with matte moon, stars, curved aviary ribs, foreground canopy, large `Sky & Sunny` title, both authentic portrait inhabitants, and a believable shared branch.
- [ ] Remove the repeated Meet section and all V1 gull marks, badges, generic portrait shapes, straight divider bars, hard section bands, and grid markup.
- [ ] Replace the moments grid with three branch-connected habitat openings using the real source aspect ratios, adjacent captions, and exact links.
- [ ] Use subject-following clip paths/masks specific to each portrait; retain Sunny’s feet/tail and Sky’s real diagonal branch.
- [ ] Extend progressive enhancement for one entrance and capped depth parallax; keep birds still and ensure reduced motion/JS-off renders the final state immediately.
- [ ] Verify 1440×900, 768px, and 390×844 compositions; both bird faces must appear in the first mobile viewport and the habitat must remain obvious in still screenshots.
- [ ] Re-run structural, JavaScript syntax, keyboard, exact-link, JS-off, reduced-motion, broken-image, 200% reflow, console, and source-integrity checks.
- [ ] Self-critique against the V2 acceptance list, refresh all screenshots, append evidence to the task report, and commit all V2 files plus the preserved interrupted-review fixes.
